import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  menu: {
    color: 'white',
    fontSize: 25
  },
  button: {
    position: 'absolute',
    right: 20,
    top: 20,
    zIndex: 3,
    borderColor: 'white',
  }
});